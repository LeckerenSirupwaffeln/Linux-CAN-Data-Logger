# Add the following lines for loopback-mode

auto can0
  iface can0 inet manual
  pre-up /sbin/ip link set can0 type can bitrate 1000000 loopback on
  up /sbin/ifconfig can0 up
  down /sbin/ifconfig can0 down

# Otherwise add these lines for normal-mode:

auto can0
  iface can0 inet manual
  pre-up /sbin/ip link set can0 type can bitrate 1000000 loopback off
  up /sbin/ifconfig can0 up
  down /sbin/ifconfig can0 down
